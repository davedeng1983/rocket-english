# 🚨 快速调试 - 3步找到问题

## ⚡ 第一步：打开控制台并测试（30秒）

1. **打开浏览器开发者工具**
   - 按 `F12` 或 `Cmd+Option+I`
   - 点击 **Console** 标签

2. **进行一次测试**
   - 选择一份试卷
   - 点击某个部分（例如"单项选择"）
   - 随便答几道题
   - **点击提交按钮**

3. **立即查看控制台**
   - 查找 `🔵 ========== 渲染结果页面 ==========`
   - 如果看到这个，说明代码执行到了结果页面
   - 如果没有看到，说明代码没有执行到这里

**请告诉我：你看到了什么？**

---

## ⚡ 第二步：检查网络请求（30秒）

1. **在开发者工具中，点击 Network 标签**
2. **重新提交一次**
3. **找到 `/api/exam-attempts/create` 请求**
4. **点击它，查看 Response**

**请告诉我：**
- 状态码是什么？（200 = 成功，其他 = 失败）
- Response 里有什么内容？

---

## ⚡ 第三步：检查页面内容（10秒）

**提交后，页面上显示什么？**

- [ ] 还停留在考试页面（显示题目）
- [ ] 显示加载中（转圈圈）
- [ ] 显示错误信息
- [ ] 跳转到其他页面
- [ ] 页面空白
- [ ] 显示了结果页面（但内容不对）

**请告诉我你看到的情况！**

---

## 📸 如果方便，请截图

1. 控制台的完整截图
2. 提交后页面的截图
3. Network 标签的截图（显示 API 请求）

---

## 🔍 我特别需要知道的信息

### 最关键的问题：
**提交按钮点击后，页面有任何变化吗？**
- 按钮文字变成"提交中..."了吗？
- 有任何提示信息吗？
- 页面完全没反应吗？

### 请回答：
1. 提交按钮点击后，**按钮本身有变化吗**？（比如变成灰色、显示"提交中..."）
2. 提交按钮点击后，**有任何弹窗出现吗**？（比如确认框、错误提示）
3. 提交按钮点击后，**控制台有任何输出吗**？（任何日志或错误）
4. **你用的是哪个浏览器？**（Chrome/Safari/Firefox + 版本号）

---

## 💡 临时快速测试

**在控制台执行以下代码，看看状态：**

```javascript
// 检查是否有 ExamRunner 相关的状态
console.log('检查页面状态...')
const scripts = Array.from(document.querySelectorAll('script'))
console.log('Scripts:', scripts.length)

// 检查是否有 React
console.log('React:', window.React ? '存在' : '不存在')

// 检查错误
window.addEventListener('error', (e) => {
  console.error('❌ 页面错误:', e.message, e.filename, e.lineno)
}, true)

// 检查未捕获的 Promise 错误
window.addEventListener('unhandledrejection', (e) => {
  console.error('❌ Promise 错误:', e.reason)
})
```

**执行后，告诉我输出结果！**

